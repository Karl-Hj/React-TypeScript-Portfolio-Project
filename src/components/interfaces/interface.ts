export interface SwipeElements {
  id: string;
  src: string;
  alt: string;
  destination: string;
  price: number;
  description: string;
}

export interface ImagesArray {
  images: SwipeElements[];
}
